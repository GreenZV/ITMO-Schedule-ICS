# ITMO Schedule ICS

## 🔧 Настройка парсера
1. Скачать репозиторий https://github.com/GreenZV/ITMO-Schedule-ICS
2. В файле по пути `config/__init__.py` в классе Config указать платформу на которую вы будете использовать для хранения календарей (по умолчанию стоит публичный репозиторий Github)
3. Создать свой приватный репозиторий и залить туда этот код
4. Теперь нужно установить переменные окружения, для этого открываем свой репозиторий и идём так: 

   `Settings -> Secrets and variables -> Actions`

   В блоке `Repository secrets` нажимаем `New repository secret` и поочерёдно добавляем следующие переменные:

   | Название               | Значение                                                       | В каких случаях                        |
   |------------------------|----------------------------------------------------------------|----------------------------------------|
   | USERNAME               | Номер ISU                                                      | Всегда                                 |
   | PASSWORD               | Пароль от my.itmo                                              | Всегда                                 |
   | TOKEN                  | Tokens (classic) токен для Github                              | При использовании Github для хранения  |
   | REPO                   | Путь к публичному репозиторию Github вида `username/repo_name` | При использовании Github для хранения  |
   | DROPBOX_APP_KEY        | App key токен для Dropbox                                      | При использовании Dropbox для хранения |
   | DROPBOX_APP_SECRET     | App secret токен для Dropbox                                   | При использовании Dropbox для хранения |
   | DROPBOX_REFRESH_TOKEN  | Refresh токен для Dropbox                                      | При использовании Dropbox для хранения |

5. Получение Token:
   1. Для Github:
      1. Переходим в настройки профиля
      2. Developer Settings
      3. Personal access tokens
      4. Tokens (classic)
      5. Generate new token (classic)
   2. Для Dropbox
      1. Переходим на https://www.dropbox.com/developers
      2. Входим в свой аккаунт
      3. Дальше на странице https://www.dropbox.com/developers нажимаем `Create apps`
      4. Выбираем `Scoped access`
      5. Далее выбираем `App folder`
      6. Ввводим любое название
      7. Переходим во вкладку `Permissions` и отмечаем всё
      8. Возвращаемся в `Settings`
      9. Теперь надо запустить скрипт `get_dropbox_refresh_token.py` для этого надо сначала выполнить `pip install dropbox`
      10. После запуска надо будет скопировать в консоль App key и App secret
      11. Выполняем инструкции из консоли и сохраняем наш Refresh token
   